- Goes to user position on first load?
- Updates the coordinates every time the map is open?
- Does the indicator stops when closing the during current position acquision?

- Does markers shows the accuracy radius?
- Does Project Info marker shows up?
- Does Sample Info marker shows up?
- Does <GPSInput /> marker shows up

- Does map closes when pressing android back button?
