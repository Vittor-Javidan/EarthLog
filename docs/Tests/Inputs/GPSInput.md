- 1 Click GPS:
  - Can get GPS data
  - Does colect data only for altitute if only altitude is checked?
  - Does colect data only for coordinates if only coordinates is checked? 

- Play Button GPS:
  - Can get GPS data
  - Does colect data only for altitute if only altitude is checked?
  - Does colect data only for coordinates if only coordinates is checked? 

- Can delete Altitude?
- Can delete Coordinates?

- Does GPS turns off pressed stop button?
- Does GPS turns off when screen is closed?
- Does manual input shows current GPS data?
- Does google map button works?

- Does acquire GPS data automatic when entering sample screen only?
  - project GPS cannot be triggered
  - Widget GPS cannot be triggered